import { style } from '@vanilla-extract/css';

export const timewatch = style({
    marginTop: 14,
    marginBottom: 48,
    display: 'flex',
    flexDirection: 'row',
    gap: 10,
    color: '#000',
    fontSize: 16,
});

export const colon = style({
    paddingTop: 4,
    fontSize: 24,
});
